@using Cv_Asp.net.Models.Entity
@model List<Tbl_Deneyimler>
    
<div class="resume-section-content">
    <h2 class="mb-5">Deneyim</h2>
    @foreach (var x in Model)
    {
        <div class="d-flex flex-column flex-md-row justify-content-between mb-5">
            <div class="flex-grow-1">
                <h3 class="mb-0">@x.Baslık</h3>
                <div class="subheading mb-3">@x.AltBaslık</div>
                <p>Buraya açıklama satırı gelecek sqlden düzeltiecek....</p>
            </div>
            <div class="flex-shrink-0"><span class="text-primary">@x.Tarih</span></div>
        </div>
    }
</div>
